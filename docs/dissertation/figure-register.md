| Figure ID | Research question | Source dataset | Exact filter | Metric | Sample unit | Artifact identity | Caption claim | Limitation |
|---|---|---|---|---|---|---|---|---|
| F1 (reserved) | S1-to-S2 CPU preparation-and-command-recording comparison | formal matrix: `results/_aggregate.csv` | 135 matched pairs: grid≥256; same grid, chunk, tile-weight preset, and seed | `cpu_record_avg` ratio S1/S2 | matched configuration pair | formal matrix; commit `407efde`; `dirty=true`; executable hash `e415726b9b02c3e5` | S1/S2 reported CPU preparation-and-command-recording time differed by 1.7366–9.9405× across the matched pairs. | Whole rendering paths differ in several operations; no isolated MDI causal claim. |
| F2 (reserved) | CPU/GPU trade-off in the fine-granularity stress-test configuration | formal matrix: `results/_aggregate.csv` | grid4096, chunk4, tile-weight preset50, seed42, S1/S2/S3 | `cpu_record_avg`; `gpu_exec_avg` | configuration aggregate | formal matrix; commit `407efde`; `dirty=true`; executable hash `e415726b9b02c3e5` | S3 reports 44.745µs CPU preparation-and-command-recording time and 236,173µs GPU elapsed time measured with timestamp queries. | GPU timestamp span has combined work and lacks stage-level attribution; readback is outside `cpu_record`. |
| F3 (reserved) | Scene mesh composition and GPU workload | formal matrix plus separately labelled supplementary sweep | grid4096, chunk16, tile-weight preset80, S3; seeds42,1337,9999 | `gpu_exec_avg`; cache mesh counts | configuration aggregate and cache histogram | Formal artifact and supplementary artifact must retain their own commit, dirty state, and executable hash. | seed9999 differs in GPU elapsed time and observed scene mesh composition from seeds42/1337. | Association only; supplementary sweep is a separate dataset. |
| F4 (reserved) | Buffer-update submission granularity | formal update matrix: `results/_aggregate_update.csv` | 32 chunks; all seeds and rendering paths | standalone buffer-update time | configuration aggregate | update matrix; commit `407efde`; `dirty=true`; executable hash `e415726b9b02c3e5` | Batched submit-and-wait reports 87.07µs versus 1,898.44µs for the per-chunk reference path. | Isolated microbenchmark; no full dynamic-frame inference. |

## Registration rule

Every final figure and result table must specify dataset name, configuration range, metric, sample unit, commit, dirty state, and executable hash. Keep formal matrix, repeat validation, and supplementary sweeps in separate entries.
