| Figure ID | Research question | Source dataset | Exact filter | Metric | Sample unit | Artifact identity | Caption claim | Limitation |
|---|---|---|---|---|---|---|---|---|
| F5.1 `fig5_1_cpu_record_bar.png` | S1-to-S2 CPU preparation-and-command-recording comparison | Formal matrix: `results/_aggregate.csv` | 135 matched S1/S2 pairs with grid≥256; same `grid`, `chunk`, `density` (tile-weight preset), and `seed`; `update=0` | Ratio of `cpu_record_avg` (S1/S2); project-defined CPU preparation-and-command-recording composite | Matched configuration pair | Formal raw artifacts named by the selected `file` field under `results/`; commit `407efde`; `dirty=true`; executable hash `e415726b9b02c3e5` | The distribution spans 1.7366–9.9405× across n=135 matched configuration pairs. | Whole rendering paths differ in several operations, so the figure does not isolate an MDI causal effect. |
| F5.2 `fig5_2_content_sensitivity_bar.png` | CPU/GPU timing trade-off in the fine-granularity stress-test configuration | Formal matrix: `results/_aggregate.csv` | `grid=4096`, `chunk=4`, `density=50` (tile-weight preset 50), `seed=42`, `scheme=1/2/3` (S1/S2/S3), `update=0`; 3 rows | `cpu_record_avg` and `gpu_exec_avg`; independent CPU preparation-and-command-recording and GPU timestamp-query axes | Configuration aggregate | Formal raw artifacts `results/grid4096_chunk4_dens50_scheme{1,2,3}_seed42.csv`; commit `407efde`; `dirty=true`; executable hash `e415726b9b02c3e5` | S3 records 44.745µs CPU preparation-and-command-recording time and 236,173µs GPU elapsed time measured with timestamp queries. | The GPU timestamp span combines work and lacks stage-level attribution; readback remains outside `cpu_record`. |
| F5.3 `fig5_3_triangle_count_scatter.png` | Scene mesh composition and GPU workload | Formal timing matrix: `results/_aggregate.csv`; supplementary cache-histogram artifacts: `cache/wfc_4096_{42,1337,9999}_80.bin` | Formal timing: `grid=4096`, `chunk=16`, `density=80` (tile-weight preset 80), `scheme=3` (S3), `update=0`, seeds 42/1337/9999; exactly 3 rows. Supplementary mesh composition: matching cache filename header `grid=4096`; exactly 3 binary artifacts. | Formal `gpu_exec_avg`; supplementary total triangle count computed from tile IDs 1–3 at 12 triangles and IDs 4–5 at 768 triangles | Formal configuration aggregate; supplementary cache histogram | Formal artifacts `results/grid4096_chunk16_dens80_scheme3_seed{42,1337,9999}.csv`; commit `407efde`; `dirty=true`; executable hash `e415726b9b02c3e5`. Supplementary artifacts are the three named cache binaries; their binary format contains the grid header and tile payload only, with no embedded commit, dirty-state, or executable hash metadata. | One composite paired glyph uses the supplementary cache-histogram triangle count on x and formal GPU timing on y; seed pairs the artifacts. Formal seed9999 timing is 600,999µs, 11.37× seed42 and 11.36× seed1337. | This is a seed-paired cross-artifact display with no pooled fit, causal attribution, or cross-tier aggregate; the inset only enlarges the unchanged seed42/seed1337 coordinates. |
| F5.4 `fig5_4_buffer_update_submission.png` | Buffer-update submission granularity | Formal update matrix: `results/_aggregate_update.csv` | `grid=128`, `chunk=8`, `density=50` (tile-weight preset 50), all seeds 42/1337/9999, `scheme=1/2/3` (S1/S2/S3), `update_size=1/2/4/8/16/32`, `mode=batched/perchunk`; 9 configuration aggregates per update-size/mode and 108 rows total | `update_cost_avg`, standalone buffer-update time | Configuration aggregate; each visible point is one aggregate and each line is its nine-aggregate arithmetic mean | Formal raw update artifacts named by the selected `file` field under `results/`; commit `407efde`; `dirty=true`; executable hash `e415726b9b02c3e5` | At 32 chunks, batched submit-and-wait is 87.07µs versus 1,898.44µs for the per-chunk reference path, a 21.80× comparison. | This isolated standalone buffer-update microbenchmark does not support a full dynamic-frame inference. |

## Registration rule

Every final figure and result table must specify dataset name, configuration range, metric, sample unit, commit, dirty state, and executable hash. Keep formal matrix, repeat validation, and supplementary sweeps in separate entries.
